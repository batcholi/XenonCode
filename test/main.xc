// var $a:number

init
	output.0 ("Hello, World!")
	
	// Note that indentations must be tabs, not spaces

	// $a = 5
	
	// if $a == 5
	// 	output.0 (text("The integer value of $a is {0} and its decimal value is {00.00}", $a, $a))
	// else
	// 	output.0 ("There must be a bug somewhere...")
